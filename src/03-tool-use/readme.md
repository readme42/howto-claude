By default, claude has only access to information it was trained on.
If we now want to build an app with claude as brain, it is quite limited when it comes reaching to things outside. 

Prompts like
- What's the current weather in Munich
- Summarize the content of this file

cant be answered by default. To fix this, we can use so called "tools".